query GetTags($name: String!) {
  BuiltinTag(name__value: $name) {
    edges {
      node {
        id
        name {
          value
        }
        description {
          value
        }
      }
    }
  }
}
