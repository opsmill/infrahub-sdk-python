Fixed the rendering of schema load errors located below an attribute or relationship, such as an invalid `parameters.regex` or a dropdown `choices[1].label`, which showed only the attribute name and dropped the failing field. The field path is now displayed after the attribute. An error on an item of a list field such as `display_labels` no longer crashes `infrahubctl schema load` and is rendered with its index.
