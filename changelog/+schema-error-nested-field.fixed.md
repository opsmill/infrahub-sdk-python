Fixed the rendering of schema load errors located below an attribute or relationship, which named the attribute but dropped the failing field (for example `parameters.regex`). An error on an item of a list field such as `display_labels` no longer crashes `infrahubctl schema load`.
