Fixed `infrahubctl schema load` and `infrahubctl schema check`, which printed only `Unable to load the schema:` with no detail when an Infrahub 1.11.0+ server rejected a schema. Each violation is now rendered on its own line, with the node, field and submitted value when it resolves to one, and with its schema file otherwise.
