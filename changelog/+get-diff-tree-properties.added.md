Added an `include_properties` argument to `client.get_diff_tree()` (async and sync). When set to `True`, the returned diff tree includes the value-level details of each change: previous/new value per attribute property, and peer id/label per relationship element. This restores the data-level diff that `client.branch.diff_data()` was meant to provide, through the `DiffTree` GraphQL query.
